# Spec

## Requirements

### Requirement: Example artifacts are understandable

The example SHALL show the minimum artifact structure for a simple Spec to Ship change.

#### Scenario: Contributor reads example
- Given a contributor opens `examples/basic-change`
- When they read the artifacts
- Then they can identify proposal, spec, design, tasks, verify, and release documents
